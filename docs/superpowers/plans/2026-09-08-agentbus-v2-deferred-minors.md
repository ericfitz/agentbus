# Deferred minors from the v2 implementation review

Non-blocking findings left open when v2 merged to main on 2026-09-08. Line
numbers are as of the review commit named in each entry and may have moved.

31:Task 1: review (head 507a093): Important — embedding_endpoint not URL-validated (config.go:248). Minor (deferred): empty config file yields "<path>: EOF"; trailing content after JSON object ignored. Reviewer report truncated after Minor 2; remainder requested.
35:Task 1: minor (deferred): AGENTBUS_DATA_DIR used verbatim (no ~/ expansion, relative vs cwd) — plan-level; test drops os.UserHomeDir/Load errors at config_test.go:325,330; go.mod pins go 1.27.1 (1.27 conventional); coverage gaps: wrong-type, log_level enum, count ordering, inspection_command bounds, --config precedence.
42:Task 2: review (head d27ccf5): Critical — auto_vacuum never takes effect (WAL DSN pragma writes page 1 first); Important — modernc.org/sqlite marked indirect in go.mod; Important — b.Close() error dropped mid-test. Minor (deferred): DSN path not URI-escaped (dirs with ? or #); owner token 12 bytes vs brief's 16; coverage gaps busy_timeout/foreign_keys/indexes/leases seed. ⚠️ no AFTER UPDATE FTS trigger — resolved: later tasks never update content in place (edits insert a new revision row); watch in Task 7 review.
46:Task 2: minor (deferred): auto_vacuum conversion relies on database/sql LIFO pool reuse rather than an explicit db.Conn pin; add a comment or pin if Open ever runs concurrently.
50:Task 3: review clean. Minor (deferred): parent not validated; "discovery disabled" uses code validation (comment it); Resumed derived from subscription rows; tests drop Open/Exec errors at sessions_test.go:219,240-242,248; suffix search one SELECT per candidate. ⚠️ resolved as later-task work: context default + 10 s ticker are Task 12; Register lists subscriptions regardless of last_activity — Task 6 receive expiry and Task 10 reaping cover it; watch at those reviews.
55:Task 4: review (head b8c10f8): Important — History `before` trims the wrong end after reversing (plan-mandated); Important — sql.ErrNoRows conflated with DB errors in channel lookup and CreateChannel (plan-mandated). Ruling: fix both; spec governs result correctness and error codes. Minor (deferred): envelope probe / stored bytes undercount (omit seq, created_at, memory_id, revision); history on nonexistent channel returns [] not not_found (spec ambiguity — Ruling: keep [] ; a missing channel has no history; cost if wrong: MCP layer surfaces empty instead of not_found); validation reads outside write tx. ⚠️ 4 MiB hard ceiling has no caller-supplied size yet — resolved: applies when Task 6/12 introduce per-call sizes; watch there.
57:Task 4: minor (deferred): coverage gaps — after, before+after, trimBytes, channel name with / or control char; tests ignore CreateChannel return at messages_test.go:371,399,417. Validation reads outside tx — fold into tx in Task 5 (carry).
60:Task 4: minor (deferred): History with both before and after keeps the lowest-seq page (nearest after); pre-existing plan behavior.
64:Task 5: review (head 47464ed): Important — fingerprint drops json.Marshal error (receipts.go:19). Minor (deferred): senderContext now runs inside the open tx on a second pool connection — move before Begin; tests cover only the message-count bucket (no byte bucket, no replay-not-charged test). ⚠️ resolved: send_messages_per_second min is 1 in config so allow never refuses forever; expired receipt purge is a Task 10 tick step.
66:Task 5: minor (deferred): no tests for byte bucket / replay-not-charged / conflict-not-charged; tests drop CreateChannel and Scan errors at receipts_test.go:12,24,44; limiter bucket map never evicted (ponytail note).
74:Task 6: minor (deferred): tests ignore Send/Subscribe/CreateChannel errors at receive_test.go:22-23,50-53,72-78; no tests for redelivery after partial eviction or unsubscribe dropping a pending batch; one SELECT evicted_before_seq per subscription in gap loop.
86:Task 7: review (head ba35e12): spec ✅, no Critical/Important. Minor (deferred): GetMemory lacks ORDER BY revision DESC LIMIT 1; rate-limit error text says "send" for edit/delete; duplicate empty-content check; replay of a keyed edit after delete returns not_found instead of the stored result (consequence of P1 step 1 ordering — final review to triage whether the outside-tx receipt check should precede the live lookup); tests drop errors at memories_test.go:266,296,317,322 and skip conflict/delete-idempotency/replay-not-charged cases. ⚠️ resolved: FTS tombstone filtering is Task 8's join; max_message_kib enforced via validateSend.
90:Task 8: review clean. Ruling: the spec's "search page 100 / max 1000" is read as the plan does — the ranking is recomputed over the top 1000 candidates and pages walk that list; results beyond rank 1000 are unreachable. Accepted; cost if wrong: a ponytail ceiling on very large memory sets. Minor (deferred): every page re-fetches/decodes 1000 rows (add ponytail note); dead fq=="" guard at search.go:199; ORDER BY rank idiom; tests drop setup errors at search_test.go:309-315 and lack since/until, cursor validation, invalid mode, score-order assertions. ⚠️ 4 MiB hard ceiling: same as History — single oversize record passes; config caps result_default_kib at 4096 so the ceiling is enforced by config.
96:Task 9: minor (deferred): DELETE stale-model embeddings on every batch pass (embed.go:156) — move to Open or condition on a stale row; a permanently rejected text stalls the first-64 batch forever (ponytail note, carry to Task 10); no test for non-2xx/malformed fallback; tests drop os.WriteFile/Decode errors at embed_test.go:36,57,81,85.
114:Task 10: minor (deferred): Close does not wait for in-flight embedSoon goroutine (lock embedMu in Close — fold into Task 10 fix round); rows.Close repeated on three paths (maintenance.go:345,352,355); tests drop Open/Send/CreateChannel/EditMemory errors (maintenance_test.go:490-495,522-524,603); capacity test's SQLiteBudgetMiB=64 only affects notice text.
126:xfa #30: Task 6 R1/#13 fix range 8c39c5e..fa1d143 Spec ✅ by the second reviewer. Minor (deferred): TestReceivePendingBatchDoesNotBlockAChannelTrimmedOutOfItEntirely exercises initial exclusion, not the zero-row re-stamp path on redelivery; seed two-channel pending state and force whole-channel tombstoning on redelivery.
127:Task 6: fix rounds 3-4/5 (R1+#13 addressed by both reviewers, 0 open; commits 8c39c5e..fa1d143). Minor (deferred): zero-rows re-stamp fallback untested; an all-tombstoned pending batch redelivers empty with a token until acked (self-heals via instruction).
132:Task 7: cross-cutting fix round 1/5 (5 addressed — R2, R3, R4, R6a, #27; 0 open; commits 64120bd..c985fd9). R3 relies on BEGIN IMMEDIATE + in-tx auth; re-reviewer confirmed takeover cannot interleave (Register and maintenance deletes block on the write lock). Minor (deferred): R3 test is sequential and passes pre-fix; a deterministic stronger test needs a test-only hook between Begin and commit. Out-of-scope note = R6b (in flight).
138:xfa #37: Task 9 round 2 (R6b, R7) Spec ✅ by the second reviewer. Minor (deferred): TestEmbedBatchSkipsSeqTombstonedDuringHTTPCall's reached/release channels lack failure-path cancellation (bound with a test context; once-guarded deferred release before srv.Close).
143:Task 7: fix round 2/5 (C1, C2 addressed; 0 open; commits 01be60c..6781e4d). Minor (deferred, carry to Task 11 dispatch since it touches messages.go): preflight sendEnvelope maps senderContext ErrNoRows to internal instead of not_registered (messages.go:186-189); search size func omits the score field (search.go:179, bound holds barely); limit<=0 on new selection long-polls to empty (only with tiny result_default_kib).
149:Task 10: fix round 1/5 (5 addressed — T10.1, T10.2, T10.3 partial per ruling, T10.4, Close/embedMu; 0 open; commits 6781e4d..f9d06c7). Minor (deferred): evictToBudget loop checks the wall-clock deadline not tickCtx (shutdown not honored inside the capacity step); interrupted capacity sweep does not resume below budget (acceptable per spec trigger); consequence of T10.4: subscriptions of senders that never receive/re-register are never reaped (bounded by names × channels).
151:xfa #43: Task 10 fix round Spec ✅ except the two recorded human-visible deviations (T10.4 tick no longer reaps idle subscriptions; T10.3 tick deadline is best-effort for SQL). T10.3 exception wording CORRECTED: do not claim a ≤5 s / one-statement bound — the timer starts after lease acquisition and a capacity chunk can reacquire the writer for incremental_vacuum before its next deadline check; describe it as "deadline enforced between chunks and for HTTP; SQL lock waits and vacuum may overrun it, unbounded only by busy_timeout per statement". Minor (deferred): maintenance.go:33-39 comment overstates the bound; TestCloseWaitsForBackgroundEmbedding needs a deferred failure-path release before srv.Close; TestTickDeadlinePropagatesToEmbedding should assert the handler was entered.
174:xfa #58: Task 11 fix round Spec ✅ by the second reviewer. Minor (deferred): TestInspectionSerializesSameIdempotencyKey relies on two 150 ms sleeps; use an explicit hook-start/release signal.
177:Task 12: review (head cf61d8a): Important — Run cancels then Close()s without waiting for the heartbeat/tick goroutines (server.go:186-190) → add a WaitGroup; Important (ruling-mandated) — os.Getwd error dropped (server.go:97). Minor (deferred): rotatingWriter keeps a closed handle if rename/remove fails after Close (set w.f=nil); missing required non-`as` fields surface as SDK schema-validation text rather than the bus's JSON error shape (two error shapes; ruling deferred to final review — options: omitempty everywhere + bus validation, or accept); report truncated after that minor.
185:xfa #64/#65 (Task 13): Important A13.1 — Reset clears sqlite_sequence, so seqs restart at 1 and an in-flight embedding HTTP request in ANOTHER process can install an old vector on a new memory that reused the seq (admin.go:110; embed.go liveness check is by seq only). Ruling (human-visible): Reset does NOT clear sqlite_sequence — seq stays monotonic across reset (spec "wipes every table" refers to data tables; ADR 0002 rejected a generation number, and monotonic seqs need none). Cost if wrong: seqs do not restart at 1 after reset (cosmetic). Regression test: blocked HTTP → reset → recreate → unblock → the new memory has no vector from the old content. Minor (deferred): identity.go:22-30 silently ignores malformed/unreadable identity files (surface non-absence errors, validate the name); Task 13 tests skip setup errors and assert a generic post-reset error rather than not_registered.
188:Task 13: subagent review Approved (head 8abf32c). Note: the subagent judged seq reuse after reset acceptable; the xfa reviewer's A13.1 cross-process embedding race overrides that — ruling stands (keep sqlite_sequence). Minor (deferred, fold the first into the fix round): status "embedding backlog" counts every memory when no embedder is configured (print 0 / omit); err != sql.ErrNoRows vs errors.Is style; tests drop setup errors; usage printed as integer MiB.
190:xfa #74: Task 12 round 1 Spec ✅ by the second reviewer (three-fix scope). Minor (deferred): TestBackgroundLoopsStopBeforeCallerCanCloseSafely does not prove an in-flight loop operation is waited on; use a start/release signal.
194:Task 12: fix round 2/5 (M12.1-M12.3 + minors addressed; 0 open; commits b84cba6..bd5fb36). Minor (deferred): no test for the Register.Pending trim; report summarizes rather than pastes the -race/-count=10 output; result() would label a hypothetical non-bus error as validation via the middleware (wrap as internal if such a path is added).
196:xfa #76/#77 (Task 12 round 2): M12.2 REMAINS — `parent` is unbounded and copied verbatim into the display name (sessions.go:76-79,133-139), so register can return a >4 MiB `as` and Discover's first record exceeds the hard cap; the new comment claiming Sender ≤128 bytes is false. Ruling: `parent` is validated at register — at most 512 UTF-8 bytes, no control characters (it may contain `/`, being a display name) — so a display name is bounded by 512+1+128+suffix; with context ≤1 KiB every sessions/channels record is a few KiB and the first-record rule cannot breach the hard cap. Fix the false comment. No legacy rows exist (pre-release), so no migration. Cost if wrong: absurdly long parent hints rejected. Folded into the in-flight Task 13 round (sessions.go). Minor (deferred): logfile two-writer test never exceeds retention (test >4 rotations, assert the cap exactly); fileIdentity could use os.SameFile.
201:Task 13: fix round 1/5 (A13.1 + 3 minors addressed; commits bd5fb36..f7101bd) and fix round 2/5 (parent bound addressed; commits f7101bd..b08a933); 0 open. Minor (deferred): cli.validIdentityName duplicates bus.validateName (export or share); A13.1 test channel waits lack deadlines.
206:xfa #85-#87 (Task 14): Important T14.1 — redelivery assertion checks only token/flag, not that the redelivered messages equal the original batch (integration_test.go:186-198). Important T14.2 — startup Connect(context.Background()) and reset CombinedOutput are unbounded (:103, :272-275); use a bounded init context and CommandContext for reset. Both accepted → Task 14 fix round 1 (after the subagent verdict and Task 15 lands). Minor (deferred): TestHookRunsInSendingProcess proves rejection not provenance; TestMain builds an uninstrumented binary so -race does not cover the child.
219:xfa #94: Task 14 fix round Spec ✅ by the second reviewer. Minor (deferred): spawn's extra cmd.Kill/Wait on connect failure duplicates the SDK's own cleanup; guard with ProcessState or drop.
229:Task 16: fix round 1/5 (S16.1, S16.3, S16.4, hygiene addressed by both reviewers; S16.2 open — gap coverage must be gaps ∪ delivered-old seqs and batch tokens must be carried into the next receive; commits 57f657f..5d8f4b2). Minor (deferred): callSafe/p.call index Content[0] unguarded (folded into round 2); sender goroutine keeps sending after a mid-loop Fatal (folded); S16.1 ~13 s by design; report truncated after that.
236:xfa #120: Task 16 round 3 Spec ✅ by the second reviewer; S16.1-S16.4 resolved across rounds. Minor (deferred): comments still say 'exactly once' / 'byte-identical' where the union allows overlap.

# Parked / human-visible rulings
103:xfa reviewer (mellow-vole-11) catch-up Tasks 5-7 (#8): Important R1 — redelivery without own filter + 4 MiB trim can omit an external message while ack advances past it (receive.go:244-281). Ruling REVERSED: own-message resurfacing is not harmless; redelivery applies the current include_own filter and re-stamps pending_end_seq to the highest redelivered seq if trimmed. Cost if wrong: own rows may be dropped/added on redelivery; external rows never lost. Important R2 — mutable-state validation precedes the receipt lookup in Send (messages.go:180-188) and Edit/Delete (memories.go:76-89): keyed edit→delete→retry returns not_found; keyed Send retry fails after reply_to eviction. Ruling: receipt check moves to right after auth + payload-shape validation, before channel/reply/live-memory lookups (amends P1 step 1-2 order). Cost if wrong: none material.
106:xfa reviewer catch-up #10/#11: Important R3 — owner auth is a preflight read only, never re-checked inside the mutating tx; a stalled long-poll or send after a name takeover acts as the replacement owner (receive.go:84,104,127; messages.go:177,201; Edit/Delete/CreateChannel/Subscribe/Unsubscribe; senderContext not owner-qualified). Ruling: accepted — auth (and senderContext) take a queryRower and are re-run inside every mutating tx right after Begin, including each receiveOnce iteration; preflight check kept. Important R4 — max_message_kib probe uses a 128-byte placeholder context while Register accepts any length (messages.go:113); byte bucket omits type/channel/sender/context. Ruling: accepted — bound `context` at Register to ≤128 UTF-8 bytes with no control characters (validation), so the probe bound holds; charge the byte bucket with the actual complete envelope. Cost if wrong: long context hints rejected (they default to basename anyway). Important R5 — trimToBytes always emits the first oversized record and ignores result framing. Ruling: PARKED — emitting one oversized record is deliberate (a record above the soft ceiling must still be deliverable or the cursor wedges; max_message_kib may exceed result_default_kib by config); framing overhead is small vs a 64 KiB ceiling "sized for a context window". Add a comment naming this. Cost if wrong: a result modestly over the soft ceiling. Final review to triage.
115:xfa #16: R4 ruling AMENDED — no context bound at Register (not in spec; escaping defeats a byte bound). Instead measure the actual complete envelope with the real sender/context/channel/type and fixed upper bounds for seq (20 digits) and created_at, check it against max_message_kib inside the write tx (a preflight check with the owner-qualified senderContext may run earlier), and charge the byte bucket with that same size. R5 ruling stands (PARKED, product decision surfaced to the user): a single record above result_default_kib is still emitted so the cursor cannot wedge; the 4 MiB hard ceiling is enforced — max_message_kib ≤ 1024 KiB so no single record can exceed it and list results are trimmed at 4 MiB. Cost if wrong: a result modestly above the soft ceiling.
116:xfa #17: R8 — search caps the ranking at 1000 candidates, not just the page. Ruling stands (accepted, plan-mandated, surfaced to the user): results beyond rank 1000 are unreachable. Minor: sessions.go:79 counts expired subscriptions in resumed pending → folded into T10.4 fix.
117:xfa #19/#20 Task 10: T10.1 — evictToBudget runs whenever usage > target, evicting below budget. Ruling: accepted — a sweep starts only when usage > budget and then continues to target; notice clearing independent. T10.2 = subagent Important (chunk the three deletes), accepted. T10.3 — tick deadline not propagated to embedBatch/HTTP. Ruling: accepted in part — Tick derives a ctx with the deadline and passes it to embedBatch and checks it between chunks; SQL is not converted to context-aware APIs (chunks are short). Cost if wrong: one chunk of overrun. T10.4 — tick reaping idle subscriptions erases the evidence Receive needs for the `expired` notice. Ruling (surfaced to the user): the tick does NOT delete idle subscriptions; Receive reaps and reports them, and Register(resume) drops expired ones and excludes them from pending counts. Subscription rows are tiny and bounded by sender×channel; the notice contract wins. Cost if wrong: stale subscription rows persist until the sender next receives or re-registers.
120:xfa #24: Task 12 must enforce the 4 MiB hard ceiling on the actual serialized tool result (drop whole records from the tail until it fits) — carry into the Task 12 dispatch. R5/R8 remain human-visible rulings.
151:xfa #43: Task 10 fix round Spec ✅ except the two recorded human-visible deviations (T10.4 tick no longer reaps idle subscriptions; T10.3 tick deadline is best-effort for SQL). T10.3 exception wording CORRECTED: do not claim a ≤5 s / one-statement bound — the timer starts after lease acquisition and a capacity chunk can reacquire the writer for incremental_vacuum before its next deadline check; describe it as "deadline enforced between chunks and for HTTP; SQL lock waits and vacuum may overrun it, unbounded only by busy_timeout per statement". Minor (deferred): maintenance.go:33-39 comment overstates the bound; TestCloseWaitsForBackgroundEmbedding needs a deferred failure-path release before srv.Close; TestTickDeadlinePropagatesToEmbedding should assert the handler was entered.
182:xfa #61-#63 (Task 12): Important M12.1 — agentbus.log is shared by every mcp process but lock, cached size, and descriptor are process-local; after A rotates, B keeps appending to the archived inode. Ruling: accepted — writes use O_APPEND; before each write stat the path (size and inode) and reopen if the inode changed; rotation (rename chain) happens under an flock on <data>/agentbus.log.lock (syscall.Flock, darwin+linux) with a re-stat inside the lock; keep four 16 MiB files. Cost if wrong: a few lost log lines under heavy contention. Important M12.2 — ListChannels/Discover/Register.Pending arrays are unbounded and an unbounded register `context` makes Discover exceed 4 MiB. Ruling (human-visible; amends R4's "no context bound"): `context` is bounded to 1024 UTF-8 bytes with no control characters at register (validation) — it is a hint, and this also closes the M12.2 blow-up path; additionally ListChannels/Discover/Register.Pending are trimmed at the bus with the whole-record helper against min(result_default_kib, 4 MiB), oversized-single-record handled by the same first-record rule. Important M12.3 — SDK typed AddTool rejects missing/wrong-typed args with plaintext errors lacking {code,message,retryable}. Ruling: accepted — normalize at the MCP boundary: if go-sdk v1.7.0 exposes a tool/receiving middleware that can rewrite the CallToolResult, wrap schema-validation failures into the JSON envelope with code `validation`; otherwise mark all input fields optional (omitempty) so required-field checks reach the bus (JSON envelope) and document wrong-type arguments as the one SDK-shaped error. Minor: bus.Open failure at startup not logged (log via the file if it opened, else the single stderr line); logfile_test ignores Write errors and only counts files.
185:xfa #64/#65 (Task 13): Important A13.1 — Reset clears sqlite_sequence, so seqs restart at 1 and an in-flight embedding HTTP request in ANOTHER process can install an old vector on a new memory that reused the seq (admin.go:110; embed.go liveness check is by seq only). Ruling (human-visible): Reset does NOT clear sqlite_sequence — seq stays monotonic across reset (spec "wipes every table" refers to data tables; ADR 0002 rejected a generation number, and monotonic seqs need none). Cost if wrong: seqs do not restart at 1 after reset (cosmetic). Regression test: blocked HTTP → reset → recreate → unblock → the new memory has no vector from the old content. Minor (deferred): identity.go:22-30 silently ignores malformed/unreadable identity files (surface non-absence errors, validate the name); Task 13 tests skip setup errors and assert a generic post-reset error rather than not_registered.

# Resolved 2026-09-08 (runtime-behaviour minors)

- GetMemory now selects `ORDER BY revision DESC LIMIT 1` (Task 7 minor).
- sendEnvelope maps a missing session (sql.ErrNoRows) to `not_registered`
  instead of `internal` (Task 7 fix round 2 minor).
- `cli.validIdentityName` removed; `bus.NameRule` is the single name rule
  behind both `validateName` and the CLI (Task 13 minor).
- Already fixed before this pass, no change needed: identity.go surfaces
  malformed/unreadable identity files and validates the name; status prints
  a zero embedding backlog when no embedder is configured.
